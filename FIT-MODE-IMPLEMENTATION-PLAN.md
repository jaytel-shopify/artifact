# "Fit to Viewport" Feature - Implementation Plan

## 🔍 Current DOM Structure Audit

### **Layout Hierarchy** (Top → Down):

```
AppLayout (h-screen)
└── Canvas container (h-full, pt-[spacing])
    └── D
